import { failedImportNamed } from '../env/node';

const failedImport  = require('../env/noddfsfe.js');
const failedImport2 = require('../env/node.js');

const test = (test) => {
  if (var1 == 'test') {
    const test1 = true;
    const test2 = false;
  }
  console.log('failedImportNamed :', failedImportNamed);
};

const testObject = {
  test: 'fdsfg',
  foo:  'bar',
};
